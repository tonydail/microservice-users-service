version: "3.9"

services:
  app:
    image: mcr.microsoft.com/devcontainers/typescript-node:1-20-bullseye
    container_name: ${USERS_APP_HOST}
    volumes:
      - ..:/microservice-users-service:cached
      - microservice_users_service_node_modules:/microservice-users-service/node_modules
      - microservice_users_service_dist:/microservice-users-service/dist
    working_dir: /microservice-users-service
    command: sleep infinity
    env_file:
      - ../../environment/container-hosts.env
      - ../../environment/common-service-db.env
      - ../../environment/microservice-users-service-db.env
      - ../../environment/common-services-app.env
      - ../../environment/microservice-users-service-app.env
      - ../../environment/common-services-app-tooling.env
    depends_on:
      users-db:
        condition: service_healthy
    networks:
      - microservices-net

  users-db:
    image: postgres:16-alpine
    container_name: ${USERS_DB_HOST}
    restart: unless-stopped
    env_file:
      - ../../environment/container-hosts.env
      - ../../environment/common-service-db.env
      - ../../environment/microservice-users-service-db.env
    ports:
      - "5434:5432"
    volumes:
      - users-db-data:/var/lib/postgresql/data
    # Enable logical replication for Debezium CDC
    command: postgres -c wal_level=logical -c max_replication_slots=4 -c max_wal_senders=4
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U users_user -d users_db"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - microservices-net

volumes:
  users-db-data:
  microservice_users_service_node_modules:
  microservice_users_service_dist:

networks:
  microservices-net:
    external: true
    name: microservices-net
